# 📚 Spring Core 학습 정리 (2026-01-15)

## 🎯 오늘 학습한 내용 요약

오늘은 **Spring Boot 기반의 회원 관리 시스템**을 구축하며 객체지향 설계 원칙과 Spring의 기초를 학습했습니다.

---

## 📁 프로젝트 구조

```
hello.core
├── 📄 CoreApplication.java          [Spring Boot 메인 클래스]
├── 📄 AppConfig.java                [Spring 설정 클래스 - 수동 빈 등록]
├── 📄 AutoAppConfig.java            [자동 컴포넌트 스캔 설정]
│
├── 📂 member/                       [회원 도메인]
│   ├── Grade.java                   [회원 등급 ENUM]
│   ├── Member.java                  [회원 엔티티]
│   ├── MemberRepository.java        [저장소 인터페이스]
│   ├── MemoryMemberRepository.java  [메모리 저장소 구현체]
│   ├── MemberService.java           [서비스 인터페이스]
│   └── MemberServiceImpl.java       [서비스 구현체]
│
├── 📂 order/                        [주문 도메인 - 진행중]
│   ├── Order.java                   [주문 엔티티]
│   └── MemberApp.java               [테스트 실행 클래스]
│
└── 📂 discount/                     [할인 정책]
    └── DiscountPolicy.java          [할인 정책 (작성중)]
```

---

## 💡 핵심 개념 학습

### 1. 계층형 아키텍처 (Layered Architecture)

```
[Presentation Layer]  ← MemberApp (실행/테스트)
         ↓
[Service Layer]       ← MemberService (비즈니스 로직)
         ↓
[Repository Layer]    ← MemberRepository (데이터 접근)
         ↓
[Data Storage]        ← HashMap (메모리 저장소)
```

**왜 이렇게 나누나요?**
- 각 계층이 독립적으로 변경 가능
- 테스트가 쉬워짐
- 코드 재사용성 향상
- 유지보수가 편리함

---

### 2. 인터페이스 기반 설계 (Interface-Based Design)

#### 📌 MemberRepository 인터페이스
```java
public interface MemberRepository {
    void save(Member member);
    Member findById(Long memberId);
}
```

#### 📌 MemoryMemberRepository 구현체
```java
public class MemoryMemberRepository implements MemberRepository {
    private static final Map<Long, Member> store = new HashMap<>();
    
    @Override
    public void save(Member member) {
        store.put(member.getId(), member);
    }
    
    @Override
    public Member findById(Long memberId) {
        return store.get(memberId);
    }
}
```

**💡 왜 인터페이스를 사용하나요?**
1. **추상화**: 구현 세부사항을 숨김
2. **유연성**: 구현체를 쉽게 교체 가능
   - MemoryMemberRepository → JdbcMemberRepository
   - MemoryMemberRepository → JpaMemberRepository
3. **다형성**: 같은 인터페이스로 다양한 구현체 사용
4. **테스트**: Mock 객체로 쉽게 테스트 가능

---

### 3. 도메인 모델 설계

#### 📌 Member 엔티티 (회원)
```java
public class Member {
    private final Long id;        // 회원 고유번호
    private final String name;    // 회원 이름
    private final Grade grade;    // 회원 등급
    
    // 생성자, Getter...
}
```

**특징:**
- `final` 키워드로 **불변 객체** 구현
- 한번 생성되면 값 변경 불가 (Immutable)
- 멀티스레드 환경에서 안전
- Setter가 없어 의도치 않은 변경 방지

#### 📌 Grade 열거형 (회원 등급)
```java
public enum Grade {
    BASIC,  // 일반 회원
    VIP     // VIP 회원
}
```

**Enum의 장점:**
- 타입 안전성 보장 (잘못된 값 입력 방지)
- 코드 가독성 향상
- IDE의 자동완성 지원

---

### 4. 서비스 계층 설계

#### 📌 MemberService 인터페이스
```java
public interface MemberService {
    void join(Member member);           // 회원 가입
    Member findMember(Long memberId);   // 회원 조회
}
```

#### 📌 MemberServiceImpl 구현체
```java
public class MemberServiceImpl implements MemberService {
    private final MemberRepository memberRepository = new MemoryMemberRepository();
    
    @Override
    public void join(Member member) {
        memberRepository.save(member);
    }
    
    @Override
    public Member findMember(Long memberId) {
        return memberRepository.findById(memberId);
    }
}
```

**⚠️ 현재 설계의 문제점:**
```java
// MemberServiceImpl 내부에서 직접 구현체 생성
private final MemberRepository memberRepository = new MemoryMemberRepository();
```

**문제:**
- MemberServiceImpl이 MemoryMemberRepository에 **강하게 결합**됨
- 다른 저장소로 변경하려면 **코드 수정 필요**
- **OCP(개방-폐쇄 원칙) 위반**
  - 확장에는 열려있어야 하지만, 변경에는 닫혀있어야 함

**해결책: 의존성 주입 (DI)**
```java
// 생성자를 통해 외부에서 주입받기
public class MemberServiceImpl implements MemberService {
    private final MemberRepository memberRepository;
    
    public MemberServiceImpl(MemberRepository memberRepository) {
        this.memberRepository = memberRepository;
    }
}
```

---

### 5. Spring 설정 방법 (2가지)

#### 방법 1: 수동 빈 등록 (AppConfig.java)
```java
@Configuration
public class AppConfig {
    
    @Bean
    public MemberService memberService() {
        return new MemberServiceImpl(memberRepository());
    }
    
    @Bean
    public MemberRepository memberRepository() {
        return new MemoryMemberRepository();
    }
}
```

**특징:**
- 명시적으로 빈을 등록
- 설정 정보를 한눈에 파악 가능
- 외부 라이브러리를 빈으로 등록할 때 유용

#### 방법 2: 자동 컴포넌트 스캔 (AutoAppConfig.java)
```java
@Configuration
@ComponentScan(
    excludeFilters = @ComponentScan.Filter(
        type = FilterType.ANNOTATION, 
        classes = Configuration.class
    )
)
public class AutoAppConfig {
}
```

**특징:**
- `@Component` 어노테이션이 붙은 클래스를 자동으로 빈 등록
- 편리하지만 어떤 빈이 등록되는지 파악하기 어려울 수 있음
- `@Controller`, `@Service`, `@Repository`도 `@Component`를 포함

---

## 🧪 테스트 코드 작성

### MemberServiceTest.java
```java
public class MemberServiceTest {
    MemberService memberService = new MemberServiceImpl();
    
    @Test
    void join() {
        //given (준비 단계)
        Member member = new Member(1L, "memberA", Grade.VIP);
        
        //when (실행 단계)
        memberService.join(member);
        Member findMember = memberService.findMember(1L);
        
        //then (검증 단계)
        Assertions.assertThat(member).isEqualTo(findMember);
    }
}
```

**Given-When-Then 패턴:**
1. **Given**: 테스트에 필요한 데이터 준비
2. **When**: 실제 테스트할 동작 실행
3. **Then**: 결과 검증

**사용한 테스트 도구:**
- **JUnit 5**: Java 테스트 프레임워크
- **AssertJ**: 가독성 좋은 검증 라이브러리
  - `assertThat(actual).isEqualTo(expected)`

---

## 🔧 오늘 수정한 오류들

### 오류 1: MemberService 익명 클래스 생성 시도
```java
// ❌ 잘못된 코드
MemberService memberService = new MemberService() {
}
```
**문제:** 인터페이스는 직접 인스턴스화할 수 없음

**해결:**
```java
// ✅ 올바른 코드
MemberService memberService = new MemberServiceImpl();
```

---

### 오류 2: Assertions 메서드 철자 오류
```java
// ❌ 잘못된 코드
Assertions.asserThat(member).isEqualTo(findMember);
```
**문제:** `asserThat` → `assertThat` (철자 오류)

**해결:**
```java
// ✅ 올바른 코드
Assertions.assertThat(member).isEqualTo(findMember);
```

---

### 오류 3: 잘못된 Assertions import
```java
// ❌ 잘못된 import
import org.junit.jupiter.api.Assertions;
```
**문제:** JUnit의 Assertions는 `assertThat` 메서드가 없음

**해결:**
```java
// ✅ 올바른 import
import org.assertj.core.api.Assertions;
```

---

### 오류 4: DiscountPolicy.java 파일 오류
```java
// ❌ 잘못된 코드
ipmort hello.core.member.Member;  // import 철자 오류

public class FixDiscountPolicy implements DiscountPolicy {
    // DiscountPolicy 인터페이스가 정의되지 않음
}
```

**문제:**
1. `ipmort` → `import` (철자 오류)
2. `Grade`를 import 하지 않음
3. `DiscountPolicy` 인터페이스가 먼저 정의되어야 함

---

## 🎓 학습한 객체지향 설계 원칙 (SOLID)

### 1. SRP (Single Responsibility Principle) - 단일 책임 원칙
- **한 클래스는 하나의 책임만 가져야 함**
- `MemberService`: 회원 비즈니스 로직만 담당
- `MemberRepository`: 데이터 저장/조회만 담당

### 2. OCP (Open-Closed Principle) - 개방-폐쇄 원칙
- **확장에는 열려있고, 변경에는 닫혀있어야 함**
- 인터페이스를 사용하면 구현체 변경 시 클라이언트 코드 수정 불필요
- ⚠️ 현재 코드는 DI가 없어서 OCP 위반 상태

### 3. LSP (Liskov Substitution Principle) - 리스코프 치환 원칙
- **하위 타입은 상위 타입을 대체할 수 있어야 함**
- `MemberRepository` 인터페이스로 어떤 구현체든 사용 가능

### 4. ISP (Interface Segregation Principle) - 인터페이스 분리 원칙
- **클라이언트는 사용하지 않는 메서드에 의존하지 않아야 함**
- `MemberRepository`는 필요한 메서드만 정의 (save, findById)

### 5. DIP (Dependency Inversion Principle) - 의존관계 역전 원칙
- **구체화에 의존하지 말고 추상화에 의존해야 함**
- ⚠️ 현재 코드는 `new MemoryMemberRepository()`로 구체 클래스에 의존 중
- Spring의 DI를 통해 해결 예정

---

## 🚀 다음 학습 계획

### 1. Order(주문) 도메인 완성
현재 Order.java가 작성 중입니다:
```java
public class Order {
    private Long memberId;
    // TODO: 추가 필드 필요
    // - String itemName (상품명)
    // - int itemPrice (상품 가격)
    // - int discountPrice (할인 금액)
}
```

### 2. 할인 정책 구현
- `DiscountPolicy` 인터페이스 정의
- `FixDiscountPolicy`: 정액 할인 (1000원)
- `RateDiscountPolicy`: 정률 할인 (10%)

### 3. 주문 서비스 구현
- `OrderService` 인터페이스
- `OrderServiceImpl` 구현체
- 회원 등급에 따라 할인 정책 적용

### 4. 의존성 주입 (DI) 적용
- 생성자 주입 방식 학습
- `@Autowired` 어노테이션 학습
- Spring 컨테이너를 통한 빈 관리

### 5. 다양한 테스트 작성
- 주문 서비스 테스트
- 할인 정책 테스트
- 통합 테스트

---

## 📊 현재 프로젝트 완성도

```
회원 도메인:    ████████████████████ 100% ✅
주문 도메인:    ████░░░░░░░░░░░░░░░░  20% 🚧
할인 도메인:    ██░░░░░░░░░░░░░░░░░░  10% 🚧
테스트 코드:    ████████░░░░░░░░░░░░  40% 🚧
Spring 통합:    ░░░░░░░░░░░░░░░░░░░░   0% ⏳
```

---

## 🤔 학습 중 궁금했던 점들

### Q1. 왜 인터페이스와 구현체를 분리하나요?
**A:** 
- 변경에 유연하게 대응하기 위해
- 나중에 메모리 DB → 실제 DB로 변경할 때 인터페이스는 그대로 두고 구현체만 교체
- 테스트할 때 가짜 객체(Mock)로 쉽게 대체 가능

### Q2. static final Map은 왜 사용하나요?
**A:**
- `static`: 모든 인스턴스가 같은 저장소를 공유
- `final`: 한번 할당된 HashMap 객체를 다른 객체로 교체 불가
- ⚠️ 주의: HashMap 자체의 값은 변경 가능 (put, remove 등)

### Q3. JUnit과 AssertJ의 차이는?
**A:**
- **JUnit**: 테스트 프레임워크 (테스트 실행, @Test 어노테이션 등)
- **AssertJ**: 검증 라이브러리 (가독성 좋은 assert 메서드 제공)
- 함께 사용하면 시너지!

### Q4. Grade를 문자열이 아닌 Enum으로 만든 이유는?
**A:**
- 문자열은 오타 가능: "VIP", "vip", "Vip" 모두 다름
- Enum은 컴파일 시점에 오류 체크
- IDE 자동완성 지원
- Switch-case 문에서 모든 케이스를 다뤘는지 확인 가능

---

## 💻 실습 예제

### 예제 1: 회원 가입 및 조회
```java
public class MemberApp {
    public static void main(String[] args) {
        // 1. 서비스 생성
        MemberService memberService = new MemberServiceImpl();
        
        // 2. VIP 회원 생성
        Member member = new Member(1L, "memberA", Grade.VIP);
        
        // 3. 회원 가입
        memberService.join(member);
        
        // 4. 회원 조회
        Member findMember = memberService.findMember(1L);
        
        // 5. 결과 확인
        System.out.println("new member = " + member.getName());
        System.out.println("findMember = " + findMember.getName());
    }
}
```

**실행 결과:**
```
new member = memberA
findMember = memberA
```

---

## 🔖 주요 용어 정리

| 용어 | 설명 |
|------|------|
| **도메인** | 비즈니스 영역 (회원, 주문, 할인 등) |
| **엔티티** | 데이터를 담는 객체 (Member, Order 등) |
| **Repository** | 데이터 저장소 (DB 접근 계층) |
| **Service** | 비즈니스 로직을 처리하는 계층 |
| **인터페이스** | 구현의 명세서 (추상화) |
| **구현체** | 인터페이스를 실제로 구현한 클래스 |
| **불변 객체** | 생성 후 값이 변하지 않는 객체 |
| **DI** | Dependency Injection (의존성 주입) |
| **IoC** | Inversion of Control (제어의 역전) |
| **Bean** | Spring 컨테이너가 관리하는 객체 |

---

## 📖 참고 자료

- Spring Boot 공식 문서: https://spring.io/projects/spring-boot
- JUnit 5 문서: https://junit.org/junit5/
- AssertJ 문서: https://assertj.github.io/doc/

---

## ✅ 오늘의 학습 체크리스트

- [x] Spring Boot 프로젝트 구조 이해
- [x] 계층형 아키텍처 설계
- [x] 인터페이스와 구현체 분리
- [x] 회원 도메인 완성
- [x] 테스트 코드 작성 (Given-When-Then)
- [x] 발생한 오류 해결
- [ ] 주문 도메인 완성 ⏳
- [ ] 할인 정책 구현 ⏳
- [ ] 의존성 주입 적용 ⏳

---

## 💪 다음 학습 목표

1. **Order 엔티티 완성하기**
2. **DiscountPolicy 인터페이스 정의하기**
3. **FixDiscountPolicy, RateDiscountPolicy 구현하기**
4. **OrderService 구현하기**
5. **생성자 주입 방식으로 DI 적용하기**

---

**🎉 오늘도 고생하셨습니다! 내일도 화이팅!** 💪

> "객체지향의 핵심은 다형성이다. 하지만 다형성만으로는 OCP, DIP를 지킬 수 없다. 
> 이를 해결하기 위해 Spring이 제공하는 DI와 IoC 컨테이너가 필요하다!"

